# Pi Network server-side API key (Pi Developer Portal -> your app -> API Key).
# Set this as an Environment Variable in Vercel (Project Settings -> Environment
# Variables) and/or in Netlify. Never commit the real key.
PI_API_KEY=
